const CommandeService = {
    commander: (value) => {
        //inspect the value
        return Promise.resolve(true);
    },

    annulerCommande: (value) => {
        //inspect the value
        return Promise.resolve(true);
    }
};

export default CommandeService;